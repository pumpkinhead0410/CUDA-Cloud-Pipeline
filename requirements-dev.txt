# Development & test dependencies (not installed in production image)
-r requirements.txt

# Testing
pytest>=8.2.0
pytest-cov>=5.0.0
httpx>=0.27.0          # required by FastAPI TestClient

# Linting & formatting
ruff>=0.4.0
mypy>=1.10.0

# Type stubs
types-requests>=2.31.0
